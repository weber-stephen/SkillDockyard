import { ArrowRight, Boxes, CheckCircle2, Compass, GitCompareArrows, Library, Send, Sparkles } from "lucide-react";
import { ArtifactTable } from "@/components/artifact-table";
import { ProductLink as Link } from "@/components/product-link";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { listArtifacts } from "@/lib/data";
import { getSharingMetrics } from "@/lib/sharing";
import { getProductMode } from "@/lib/product-mode";
import { getOnboardingState } from "@/lib/onboarding";
import { SetupHub } from "@/components/setup-hub";

export async function DashboardPage() {
  const [artifacts, onboarding, productMode] = await Promise.all([listArtifacts(), getOnboardingState(), getProductMode()]); const metrics = getSharingMetrics(artifacts); const spotlight = artifacts.find((item) => item.slug === "campaign-brief-builder") ?? artifacts.find((item) => item.status === "needs_reapproval"); const mode = productMode === "demo" ? "Demo data" : "Live workspace";
  if (!artifacts.length && productMode !== "demo") return <SetupHub initialState={onboarding} hasArtifacts={false} />;
  return <div className="space-y-8">{productMode !== "demo" ? <SetupHub initialState={onboarding} hasArtifacts={artifacts.length > 0} compact /> : null}<header className="grid gap-6 border-b border-border pb-8 lg:grid-cols-[1.1fr_0.9fr]"><div className="space-y-5"><div className="inline-flex items-center gap-2 rounded-sm border border-border bg-panel px-3 py-1 text-xs font-bold uppercase tracking-[0.18em] text-muted-foreground"><Library className="h-3.5 w-3.5" />{mode}</div><div className="space-y-3"><h1 className="max-w-4xl text-4xl font-black leading-[0.95] tracking-normal sm:text-6xl">Share your skill improvements without losing what changed.</h1><p className="max-w-2xl text-base leading-7 text-muted-foreground">Your library contains only skills saved to this account. Compare local improvements with the published version and keep the updates you want to reuse.</p></div><div className="flex flex-wrap gap-3"><Button asChild><Link href="/submit">Submit Skill<Send className="h-4 w-4" /></Link></Button><Button asChild variant="outline"><Link href="/getting-started">Set up library<Compass className="h-4 w-4" /></Link></Button><Button asChild variant="outline"><Link href="/artifacts">Browse Skills</Link></Button></div></div><div className="grid content-end gap-3 sm:grid-cols-2"><Metric icon={Library} label="Your skills" value={metrics.sharedSkills.toString()} /><Metric icon={Sparkles} label="Improvements available" value={metrics.improvementsAvailable.toString()} /><Metric icon={CheckCircle2} label="In sync" value={metrics.inSync.toString()} /><Metric icon={Boxes} label="New skills" value={metrics.newSkills.toString()} /></div></header>{spotlight ? <section className="grid gap-4 rounded-md border border-border bg-panel p-5 lg:grid-cols-[0.8fr_1.2fr_auto] lg:items-center"><div><Badge variant="risk">Improvement spotlight</Badge><h2 className="mt-3 text-2xl font-black">Campaign Brief Builder is ready to share.</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">The marketing team added customer-language prompts and a sales handoff. Compare the improvement, then publish the version you want to use.</p></div><div className="grid gap-3 md:grid-cols-2"><Summary title="Published version" body="A simple campaign brief with audience, message, and channel ideas." /><Summary title="Current improvement" body="Adds customer-language prompts, message variations, and sales handoff." /></div><Button asChild><Link href={`/artifacts/${spotlight.id}/review`}>Compare Versions<ArrowRight className="h-4 w-4" /></Link></Button></section> : null}<ArtifactTable artifacts={artifacts} /><section className="grid gap-4 md:grid-cols-3"><Step title="1. Submit" body="Paste a new skill or update so it does not stay trapped in chat, docs, or a local copy." href="/submit" /><Step title="2. Compare" body="Review what changed against the published version before you adopt it." /><Step title="3. Preserve" body="Publish the exact improved content hash so you can reuse the same skill later." icon={<GitCompareArrows className="h-4 w-4" />} /></section></div>;
}
function Metric({ icon: Icon, label, value }: { icon: React.ElementType; label: string; value: string }) { return <div className="rounded-md border border-border bg-panel p-4"><div className="mb-5 flex items-center justify-between text-muted-foreground"><span className="text-xs font-bold uppercase tracking-[0.16em]">{label}</span><Icon className="h-4 w-4" /></div><div className="text-4xl font-black">{value}</div></div>; }
function Summary({ title, body }: { title: string; body: string }) { return <div className="rounded-md border border-border bg-background p-4"><div className="text-xs font-bold uppercase tracking-[0.16em] text-muted-foreground">{title}</div><p className="mt-3 text-sm leading-6">{body}</p></div>; }
function Step({ title, body, icon, href }: { title: string; body: string; icon?: React.ReactNode; href?: string }) { return <div className="rounded-md border border-border bg-panel p-5"><div className="mb-8 flex items-center justify-between"><h3 className="font-black">{title}</h3>{icon}</div><p className="text-sm leading-6 text-muted-foreground">{body}</p>{href ? <Button asChild variant="outline" size="sm" className="mt-5"><Link href={href}>Open guide<Compass className="h-3.5 w-3.5" /></Link></Button> : null}</div>; }
