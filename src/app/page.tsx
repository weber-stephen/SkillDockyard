import { ProductLink as Link } from "@/components/product-link";
import { ArrowRight, CheckCircle2, Compass, GitCompareArrows, Library } from "lucide-react";
import { Button } from "@/components/ui/button";

export default function MarketingPage() {
  return <main className="min-h-screen"><header className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 sm:px-8"><Link href="/" className="flex items-center gap-3 font-black"><span className="grid h-10 w-10 place-items-center rounded-md bg-primary text-white"><Library className="h-5 w-5" /></span><span>Skill Dockyard</span></Link><nav className="flex items-center gap-3"><Link className="text-sm font-bold text-muted-foreground hover:text-foreground" href="/demo">Demo</Link><Link className="text-sm font-bold text-muted-foreground hover:text-foreground" href="/login">Log in</Link><Button asChild size="sm"><Link href="/signup">Sign up</Link></Button></nav></header><section className="mx-auto grid max-w-7xl gap-10 px-5 py-20 sm:px-8 lg:grid-cols-[1.1fr_0.9fr] lg:py-28"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-primary">A private library for AI work</p><h1 className="mt-5 max-w-4xl text-5xl font-black leading-[0.92] sm:text-7xl">Keep the skill improvements you discover.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-muted-foreground">Skill Dockyard gives you one private place to submit, compare, review, and reuse the instructions that make AI work better.</p><div className="mt-8 flex flex-wrap gap-3"><Button asChild><Link href="/signup">Create account<ArrowRight className="h-4 w-4" /></Link></Button><Button asChild variant="outline"><Link href="/demo">Explore the demo<Compass className="h-4 w-4" /></Link></Button></div><p className="mt-4 text-sm text-muted-foreground">You’ll confirm your email before your private workspace opens.</p></div><section className="grid gap-3 self-end"><Proof icon={Library} title="Collect" body="Capture skills and local improvements before they disappear into private copies." /><Proof icon={GitCompareArrows} title="Compare" body="Review the exact changes, hashes, and trust notes before publishing them to your library." /><Proof icon={CheckCircle2} title="Preserve" body="Publish the version you want to use again." /></section></section></main>;
}
function Proof({ icon: Icon, title, body }: { icon: React.ElementType; title: string; body: string }) { return <div className="rounded-md border border-border bg-panel p-5"><Icon className="h-5 w-5 text-primary" /><h2 className="mt-5 text-xl font-black">{title}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{body}</p></div>; }
